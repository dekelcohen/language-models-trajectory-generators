# Genesis pulls in taichi; torch is required but NOT pinned here since Genesis needs
# the CPU or CUDA build matching the machine (install separately, see README).
genesis-world>=1.3.3
numpy>=1.26.4
opencv_python>=4.10.0
Pillow>=10.1.0
loguru>=0.7.3